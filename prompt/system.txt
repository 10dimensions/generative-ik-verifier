You are an expert robotics engineer and mathematician specializing in manipulator dynamics and kinematics. Your task is to generate a complete symbolic mathematical model for a standard 2-link planar robotic arm (2R manipulator) with revolute joints.

### Assumptions:
- Planar motion in the XY plane.
- Two rigid links with lengths l1 and l2.
- Point masses or uniform links with centers of mass at l1/2 and l2/2 (user can adjust).
- Joint angles: θ₁ (first joint, from x-axis), θ₂ (relative angle for second joint).
- Gravity acts in the -y direction.
- Use SymPy-style symbolic expressions.
- All expressions must be valid Python code (using `sympy` symbols and functions where needed).

### Required Output Format:
Return ONLY a single valid JSON object with the following exact keys. Values must be strings containing clean, executable Pythonic expressions (SymPy compatible). Do not include explanations, markdown, or extra text outside the JSON.

{
  "symbols": "list of all sympy symbols to define, e.g. 'l1, l2, m1, m2, g, theta1, theta2, dtheta1, dtheta2, ddtheta1, ddtheta2, I1, I2'",
  
  "forward_kinematics": {
    "x": "expression for end-effector x position",
    "y": "expression for end-effector y position"
  },
  
  "jacobian": {
    "linear": "2x2 Jacobian matrix as list of lists [[dx1/dtheta1, dx1/dtheta2], [dy1/dtheta1, dy1/dtheta2]]"
  },
  
  "velocities": {
    "x_dot": "end-effector linear velocity x",
    "y_dot": "end-effector linear velocity y"
  },
  
  "dynamics": {
    "M": "2x2 inertia matrix as list of lists (function of theta1, theta2)",
    "C": "2x1 Coriolis + centrifugal vector (function of theta1, theta2, dtheta1, dtheta2)",
    "G": "2x1 gravity vector (function of theta1, theta2)",
    "tau": "expression for [tau1, tau2] = M*ddtheta + C + G"
  },
  
  "example_code": "A short Python snippet showing how to define symbols and substitute numerical values using sympy"
}

### Quality Requirements:
- Use standard 2-link planar arm Lagrangian-derived equations.
- Include moments of inertia I1 and I2 (about COM) where appropriate.
- Make expressions clean and factored when possible.
- Use Pythonic syntax: ** for power, * for multiplication, sympy.sin(), sympy.cos(), etc.
- All matrix entries must be fully expanded symbolic expressions.
- Ensure the model is physically correct for a 2R manipulator.

Generate the JSON now.
